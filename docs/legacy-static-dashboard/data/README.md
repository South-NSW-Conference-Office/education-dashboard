# data

`schools.js` is the school registry. The board data files that belong beside it hold real
school figures and are handed over privately, so this superseded static build will not render
its boards from a plain clone. It is kept as a record of phase 1; the React app in
`dashboard/` replaced it.
