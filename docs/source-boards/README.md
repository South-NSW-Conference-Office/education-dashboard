# source-boards

The five original stand-alone HTML boards lived here: the weekly education databoard and the
four school finance boards. Each has real figures baked into it, so they are handed over
privately rather than published. The React app in `dashboard/` supersedes them; they are kept
only as a reference for how each board originally read.
